{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "mysql_local": {
      "type": "local",
      "command": ["uv", "run", "src/scripts/mysql_mcp_server.py"],
      "enabled": true,
      "environment": {
        // Añadir variables de ser necesario
        "DB_HOST": "localhost"
      }
    }
  }
}